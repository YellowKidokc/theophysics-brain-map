# Protocol Vector Calibration - 2026-05-20

Calibration artifact:

```text
Backside/lossless_context_pipeline/calibration/lossless_context_protocol_v1.md
```

## Question

If another AI receives the Lossless Context Compression + Semantic Addressing Protocol, what 10-variable artifact vector should it assign?

## Expected Human/Codex Semantic Read

```text
TECH/LOSSLESS-CONTEXT-COMPRESSION-PROTOCOL/W/AI_RESEARCH/T/R1 :: G3M3E0S0T3K3R3Q0F3C3 :: C3Q0-G3S0-K3E0-M3F3-T3R3
```

Reasoning:

| Variable | Score | Reason |
|---|---:|---|
| G | 3 | Protocol defines authority, rules, required fields, and scoring law. |
| M | 3 | It is a procedure/pipeline for transforming documents. |
| E | 0 | The artifact itself is orderly; disorder is not dominant. |
| S | 0 | It is not primarily selfhood/personhood. |
| T | 3 | It has ordered parts, lifecycle states, append-only runs, and reconstruction sequence. |
| K | 3 | It is dense structured knowledge: schema, definitions, fields, equations, controlled vocabulary. |
| R | 3 | It defines dependencies between AI, documents, databases, claims, evidence, scores, and reconstruction. |
| Q | 0 | It is not primarily felt experience. |
| F | 3 | It governs trust under uncertainty: reconstruction confidence, audit risk, evidence gaps, integrity checks. |
| C | 3 | Its explicit dominant function is integration into one self-contained reconstruction artifact. |

## First Deterministic Pipeline Result

Before calibration patch:

```text
TECH/LOSSLESS-CONTEXT-COMPRESSION-SEMANTIC-ADDRESSING-PROTOCOL-V1-0/W/AI_RESEARCH/T/R1 :: G0M3E0S0T3K3R3Q0F0C3 :: C3Q0-K3S0-M3F0-T3G0-R3E0
```

The scorer missed:

- `G=3` because it did not yet detect protocol/required/rule authority language.
- `F=3` because it did not yet treat reconstruction confidence, audit risk, verification, and uncertainty as trust signals.

## Calibration Rule

For protocol/checklist/schema artifacts:

```text
G=3 when the artifact defines normative rules, required fields, controlled vocabulary, or scoring law.
F=3 when the artifact governs reconstruction confidence, audit trust, safety/risk commitment, or verification under uncertainty.
C=3 only when the artifact's dominant function is integration/synthesis into a unified object, not merely because it is organized.
```

## GPT Comparison Prompt

Ask GPT:

```text
Using only the 0/3 artifact-function rules, score the Lossless Context Compression + Semantic Addressing Protocol v1.0 across G/M/E/S/T/K/R/Q/F/C. Do not score by subject matter alone. Explain any disagreement around G, F, and C.
```

Call it aligned if GPT returns:

```text
G3M3E0S0T3K3R3Q0F3C3
```

or if it returns the same except for a well-argued `C0` under a stricter "not synthesis, only procedure" reading.
