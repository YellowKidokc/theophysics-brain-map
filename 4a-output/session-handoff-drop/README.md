# session-handoff-drop

**What this is:** Workflow folder for session handoff paste intake and transformation.
**Owner:** shared
**Status:** live
**Last updated:** 2026-05-16

Generated artifact from Prompt 4a rollout for local application on X:. Content is inferred from the 4a brief and repository context, and should be synced onto NAS as a starting contract.

If this folder contains workflow code or data outside this mirror, verify details on X:\ before finalizing owner/service metadata.

## What's inside
- `<TBD — David to confirm>` — Source tree inspection required on X:\ to enumerate current children
- `<TBD — David to confirm>` — Source tree inspection required on X:\ to enumerate current children
- `<TBD — David to confirm>` — Source tree inspection required on X:\ to enumerate current children
- `<TBD — David to confirm>` — Source tree inspection required on X:\ to enumerate current children
- `<TBD — David to confirm>` — Source tree inspection required on X:\ to enumerate current children

## Pipeline contract

- **DROP HERE:** `./00_DROP/` — <TBD — David to confirm accepted file/url inputs>
- **RUN:** `./RUN.bat` — <TBD — David to confirm execution behavior>
- **OUTPUT:** `./OUTPUT/` — <TBD — David to confirm output artifact types>
- **ARCHIVE:** `./ARCHIVE/` — Inputs move after successful run
- **CONFIG:** `./config.json` — key fields: `agent_model`, `model_endpoint`, `<TBD — David to confirm>`

## What happens

1. Intake artifacts are dropped into `00_DROP/`.
2. Workflow runtime processes input according to local pipeline logic.
3. Results are written to `OUTPUT/` and processed inputs are archived.
